import sqlite3
import pandas as pd
import numpy as np

def get_data(path_minimal_formulas_db, path_neural_nets_db):

    ####### get minimal formulas

    ####### get neural nets learning data

    # get the actual data

    # for each category for each repetition,
    # average across batches (to stabilize) and then
    # find the sum of the errors across all 
    # epochs 

