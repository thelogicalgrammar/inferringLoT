This folder is meant to contain the data so that the repo doesn't become too big.
It should contain at a minimum:
1. learning_costs.pkl (the time it took the NNs to acquire each category)
2. lengths_data.npy (the length of the minimal formula for each category)